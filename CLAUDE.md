# zebra-ide — notes for Claude sessions

- Read `README.md` first (run line, first-run checklist, file map). The gate is
  `bash tools/check.sh`; every module beyond the GUI has a headless test.
- **Crew room:** `.claude/agents/` (advocate, refuter, cleanroom, historian) and
  `.claude/crew/LOG.md`. Convene advocate + refuter before declaring a phase done;
  historian before trusting README/PLAN status; cleanroom for self-reviewed diffs.
  Playbook: `C:\Users\Sean\wiki\pages\concepts\concept_crew-room.md`.
- Compiler changes belong in `C:\Projects\zebra-language` (selfhost only); this
  repo carries workarounds with their BUG numbers beside them.
- Sean retains push on every repo; merge locally.
